# Represents a student with multiple variables
# Takes a user's name and house as input, then prints a formatted sentence showing where the person is from.
name = input("Name: ")
house = input("House: ")
print(f"{name} from {house}")