### What This Program Does

---
***Generates 10 math problems Gives 3 attempts per question Tracks score***


---
### How to Test the Program

Run the program with python3 professor.py

```
    Example:

    5 + 3 = 9 → EEE
    5 + 3 = 8 → correct
```
>Nested loops + state tracking Outer loop → number of questions Inner loop → attempts Track variables (score, attempts) Use break and loop else